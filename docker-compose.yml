services:
  trace:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - trace_data:/data
    environment:
      TRACE_BIND: 0.0.0.0:8080
      TRACE_DATA_DIR: /data
    restart: unless-stopped

volumes:
  trace_data:
